{
  "name": "object-undo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "lib": "vite build",
    "preview": "vite preview",
    "test": "vite build && jest"
  },
  "devDependencies": {
    "typescript": "^4.9.3",
    "vite": "^4.1.0"
  },
  "dependencies": {
    "jest": "^29.4.3"
  }
}
